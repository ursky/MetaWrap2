---
name: Bug report
about: Report a problem running MetaWrap2
labels: bug
---

**MetaWrap2 version** (`metawrap2 -v`):

**Module and exact command** (the top line of `run_commands.txt` in your output dir):
```
metawrap2 ...
```

**What happened vs. what you expected:**

**Error output** (paste the tail of `run.stderr` from the output directory, or the console error):
```
```

**Environment status** (`metawrap2 test <module>`):
```
```

**System:** OS, cores, RAM. How the conda envs were created (`metawrap2 install-env` / manual).
